import React from "react";
import { StyleSheet, Text, View } from "react-native";

const LoginScreen = ({ children, props }) => {
  return <Text {...props}>LoginScreen</Text>;
};

export default LoginScreen;

const styles = StyleSheet.create({});
